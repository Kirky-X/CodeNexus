[package]
name = "codenexus"
version = "0.3.12"
edition = "2021"
rust-version = "1.97.1"
description = "A queryable code knowledge graph tool built on LadybugDB and tree-sitter"
authors = ["Kirky.X"]
license = "MIT"
repository = "https://github.com/Kirky-X/codenexus"
readme = "README.md"
keywords = ["code-analysis", "knowledge-graph", "tree-sitter", "ladybugdb", "code-indexing"]
categories = ["command-line-utilities", "development-tools", "parsing"]
homepage = "https://github.com/Kirky-X/codenexus"
documentation = "https://github.com/Kirky-X/codenexus#readme"
build = "build.rs"

[lib]
name = "codenexus"
path = "src/lib.rs"

[[bin]]
name = "codenexus"
path = "src/main.rs"

[[bin]]
name = "codenexus-verify"
path = "tools/verification/src/main.rs"

[features]
default = ["full"]
minimal = ["lang-rust"]
core = ["lang-c", "lang-rust", "lang-python"]
full = ["core", "lang-fortran", "lang-typescript", "lang-go", "lang-java", "lang-cpp", "lang-javascript", "lang-ruby", "lang-haskell", "lang-ocaml", "lang-scala", "lang-php", "lang-csharp", "lang-bash", "lang-html", "lang-css", "lang-json", "lang-regex", "lang-verilog", "daemon", "analysis", "complexity", "api-review", "community", "cross-service", "diagram", "lsp", "cli", "mcp", "cache", "embed", "i18n"]
lang-c = ["dep:tree-sitter-c"]
lang-rust = ["dep:tree-sitter-rust"]
lang-fortran = ["dep:tree-sitter-fortran"]
lang-python = ["dep:tree-sitter-python"]
lang-typescript = ["dep:tree-sitter-typescript"]
lang-go = ["dep:tree-sitter-go"]
lang-java = ["dep:tree-sitter-java"]
lang-cpp = ["dep:tree-sitter-cpp"]
lang-javascript = ["dep:tree-sitter-javascript"]
lang-ruby = ["dep:tree-sitter-ruby"]
lang-haskell = ["dep:tree-sitter-haskell"]
lang-ocaml = ["dep:tree-sitter-ocaml"]
lang-scala = ["dep:tree-sitter-scala"]
lang-php = ["dep:tree-sitter-php"]
lang-csharp = ["dep:tree-sitter-c-sharp"]
lang-bash = ["dep:tree-sitter-bash"]
lang-html = ["dep:tree-sitter-html"]
lang-css = ["dep:tree-sitter-css"]
lang-json = ["dep:tree-sitter-json"]
lang-regex = ["dep:tree-sitter-regex"]
lang-verilog = ["dep:tree-sitter-verilog"]
embed = ["dep:reqwest", "dep:ort", "dep:tokenizers", "dep:ndarray"]
daemon = ["dep:notify", "dep:notify-debouncer-full", "dep:signal-hook"]
analysis = []
api-review = ["analysis"]
community = ["analysis", "dep:petgraph"]
cross-service = ["analysis"]
complexity = ["analysis"]
diagram = ["analysis"]
lsp = ["dep:lsp-types", "dep:lsp-server", "dep:crossbeam-channel", "dep:url"]
mcp = ["dep:sdforge", "sdforge/mcp", "sdforge/inklog", "dep:oxiarc-zstd"]
cli = ["dep:sdforge", "sdforge/cli", "sdforge/inklog", "dep:oxiarc-zstd"]
cache = ["dep:oxcache"]
i18n = ["dep:icu_normalizer", "dep:icu_casemap"]

[dependencies]
trait-kit = { version = "0.5.0-rc.5", default-features = false, features = ["async", "lifecycle", "health", "shutdown"] }
lbug = "0.20"
tree-sitter = "0.27"
tree-sitter-c = { version = "0.24", optional = true }
tree-sitter-rust = { version = "0.24", optional = true }
tree-sitter-fortran = { version = "0.6", optional = true }
tree-sitter-python = { version = "0.25", optional = true }
tree-sitter-typescript = { version = "0.23", optional = true }
tree-sitter-go = { version = "0.25", optional = true }
tree-sitter-java = { version = "0.23", optional = true }
tree-sitter-cpp = { version = "0.23", optional = true }
tree-sitter-javascript = { version = "0.25", optional = true }
tree-sitter-ruby = { version = "0.23", optional = true }
tree-sitter-haskell = { version = "0.23", optional = true }
tree-sitter-ocaml = { version = "0.26", optional = true }
tree-sitter-scala = { version = "0.26", optional = true }
tree-sitter-php = { version = "0.24", optional = true }
tree-sitter-c-sharp = { version = "0.23", optional = true }
tree-sitter-bash = { version = "0.25", optional = true }
tree-sitter-html = { version = "0.23", optional = true }
tree-sitter-css = { version = "0.25", optional = true }
tree-sitter-json = { version = "0.24", optional = true }
tree-sitter-regex = { version = "0.25", optional = true }
tree-sitter-verilog = { version = "1.0", optional = true }
rayon = "1.12"
ignore = "0.4"
notify = { version = "8.2", optional = true }
notify-debouncer-full = { version = "0.7", optional = true }
signal-hook = { version = "0.4", optional = true }
blake3 = { version = "1.8", default-features = false, features = ["std"] }
reqwest = { version = "0.13", default-features = false, features = ["json", "blocking", "rustls"], optional = true }
ort = { version = "2.0.0-rc.13", optional = true }
tokenizers = { version = "0.23", default-features = false, features = ["fancy-regex"], optional = true }
ndarray = { version = "0.17", optional = true }
thiserror = "2.0"
anyhow = "1.0"
pest = "2.9"
pest_derive = "2.9"
tracing = "0.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
csv = "1.4"
tempfile = "3.27"
lz4_flex = "0.14"
sysinfo = { version = "0.39", default-features = false, features = ["system", "disk"] }
uuid = { version = "1.26", features = ["v7"] }
petgraph = { version = "0.8", default-features = false, optional = true }
lsp-types = { version = "0.97", optional = true }
lsp-server = { version = "0.10", optional = true }
crossbeam-channel = { version = "0.5", optional = true }
url = { version = "2.5", optional = true }
sdforge = { version = "0.5.0-rc.4", default-features = false, optional = true }
tokio = { version = "1.53", features = ["rt-multi-thread", "macros", "io-std", "io-util"] }
oxcache = { version = "0.5.0-rc.4", default-features = false, features = ["minimal", "macros"], optional = true }
oxiarc-zstd = { version = "0.4", default-features = false, optional = true }
inklog = { version = "0.3.0-rc.4", default-features = false }
regex = "1.13"
icu_normalizer = { version = "2.3", default-features = false, features = ["compiled_data"], optional = true }
icu_casemap = { version = "2.3", default-features = false, features = ["compiled_data"], optional = true }

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }
time = { version = "0.3", features = ["macros"] }
rust_decimal = "1.43"
tokio = { version = "1.53", features = ["rt-multi-thread", "macros"] }
serial_test = "4.0"
oxiarc-zstd = { version = "0.4", default-features = false }
tracing-subscriber = "0.3"
crossbeam-channel = "0.5"

[build-dependencies]
pkg-config = "0.3"

[[bench]]
name = "index_bench"
harness = false

[[bench]]
name = "query_bench"
harness = false

[[bench]]
name = "trace_bench"
harness = false

[[bench]]
name = "incremental_bench"
harness = false

[[bench]]
name = "memory_bench"
harness = false

[[bench]]
name = "daemon_bench"
harness = false
required-features = ["daemon"]

[[bench]]
name = "graph_bench"
harness = false

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
overflow-checks = false

[profile.release.package."*"]
opt-level = 3

[profile.dev]
opt-level = 0
debug = true
incremental = true

[profile.test]
opt-level = 0
debug = true

[profile.bench]
inherits = "release"

[lints.rust]
unexpected_cfgs = "allow"

[workspace]
members = ["examples"]
# Daily commands (build/test/check) stay scoped to the main crate; examples
# join only for workspace-wide ops (cargo clean --workspace, metadata).
default-members = ["."]

# Local development: uncomment to use path deps for base libraries.
# MUST be commented out before publishing to crates.io.
# [patch.crates-io]
# trait-kit = { path = "/home/kirky/projects/base/trait-kit" }
# sdforge = { path = "/home/kirky/projects/base/sdforge" }
# oxcache = { path = "/home/kirky/projects/base/oxcache" }
# inklog = { path = "/home/kirky/projects/base/inklog" }
